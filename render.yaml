services:
  - type: web
    name: instagram-365-poster-web
    runtime: python
    plan: free
    buildCommand: "pip install -r automation/requirements.txt && playwright install chromium"
    startCommand: "gunicorn automation.app:app"
    envVars:
      - key: PORT
        value: 10000

